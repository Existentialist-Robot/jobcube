# The tooling is stdlib-only wherever it can be. These are the exceptions.
#
# plotly    — the legacy Plotly renderer behind JOB_VIZ_LEGACY=1. The current
#             three.js viz needs nothing; this is only for the fallback path.
# pymupdf   — renders exported PDF pages to PNG for the render-verify loop.
# pyyaml    — optional. tools/lint_skills.py falls back to its own frontmatter
#             reader without it, but CI installs it so both paths get exercised.

plotly>=5.20
pymupdf>=1.24
pyyaml>=6.0
